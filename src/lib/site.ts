/** Canonical origin, used for canonical/og:url tags and the sitemap. */
export const siteUrl = 'https://glennbarosen.com';

/** Every route on the site. Keep in sync when adding a page — the sitemap reads this. */
export const routes = ['/', '/about', '/experience', '/contact'];
